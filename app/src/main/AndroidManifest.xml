<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ml.lacmus.app">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="ml.lacmus.app.LacmusApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_lacmus"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_lacmus_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LacmusAndroid">
        <activity
            android:name="ml.lacmus.app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>